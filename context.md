# VibeLMS project context

## Current state

VibeLMS is an independent WordPress LMS fork based on the LifterLMS trunk source snapshot. Public LifterLMS identifiers and the `lifterlms` text domain remain unchanged for compatibility while the fork is being adapted to the project requirements.

The 0.0.51 source release localizes built-in VibeLMS front-end labels for the active language in the reusable header, footer and language switcher. The Elementor **Карточка курса** widget now supports a separate title, description and button for every configured language, while arbitrary custom text is preserved. The 0.0.50 source release keeps localized Elementor HTML safe while retaining HTTPS YouTube iframe embeds from the approved YouTube host list, so translated RU/KZ homepage blocks can keep their three videos. The 0.0.49 source release adds the Elementor **Контент по языку** widget. It renders one project-configured content block for the active VibeLMS language, supports any language returned by `vibelms_supported_languages`, and has an optional fallback language. The existing language switcher now has a reusable content counterpart; it does not translate arbitrary static Elementor text automatically. The 0.0.48 source release closes the Elementor footer content section so the reusable header/footer controls register without an editor section-state error. The 0.0.47 source release adds full Elementor content and style controls for the reusable VibeLMS header and footer: logo mode/image/text, logo destination, repeater navigation links, optional language/profile, action buttons, footer copy, support/developer links and responsive style controls. Existing LMS widgets already expose the shared `Стили VibeLMS` section, and the existing `llms-course-builder` is unchanged. The 0.0.46 source release fixes the remaining public Elementor course path: the course full-description template now resolves Elementor documents through the shared protected renderer, while the existing WordPress content path, access checks, recursion guard and diagnostic fallback remain intact.

On staging, page ID 112 is the published homepage and is built in Elementor with the VibeLMS header, a language-aware **Контент по языку** block containing RU/KZ route, video and standards copy, three approved YouTube embeds, a selected course card for course 106 and the VibeLMS footer. The former static Russian text and duplicate CTA widgets were cleared, so `?vibelms_language=kz` now renders the Kazakh block without the former Russian route copy; the selected course card remains the project-specific RU course record. The public KZ check confirmed the Kazakh route, training, video and document strings and no Russian route text. The page was visually checked on desktop; the video cards use wrapping layout so the content collapses safely on narrow screens. Course 106 remains published and contains three native Elementor YouTube Video widgets; the post-0.0.46 retest confirmed three visible YouTube iframe nodes for the enrolled learner, while actual third-party video paint remains subject to YouTube network policy. A temporary learner `vibelms_ui_qa_20260828` (user ID 3) was created and enrolled in course 106 through the штатный bulk-enrollment screen for the public Elementor verification.

The staging page ID 7 is now titled **Регистрация участника**, uses the Elementor canvas with a VibeLMS header and footer, a Russian onboarding block and the nonce-protected `[lifterlms_registration]` form. The public admin-session check confirms the page structure and form mount; field-level anonymous submission remains covered by the earlier visitor preview because the available browser session is authenticated.

Staging page ID 150 is the published **Курсы** catalog at `/obuchenie/`. It uses the reusable VibeLMS header, intro block, **Каталог курсов** widget and footer. The native LifterLMS `/courses/` archive remains untouched; the three custom VibeLMS headers now point to `/obuchenie/` so users do not land on the unstyled legacy archive.

The 0.0.45 source release fixes the public Elementor output path for VibeLMS courses, lessons and tests. When an allowed LMS post is built with Elementor, `llms_get_post_content()` now renders the saved Elementor document through `get_builder_content_for_display()` before the normal LifterLMS before/after templates; restricted content remains protected, nested renders are guarded, and rendering exceptions are recorded in opt-in diagnostics. The previous `post_content` path remains the fallback. The existing `llms-course-builder` is unchanged.

The 0.0.44 source release adds a complete-result mode to the existing Elementor **Результаты тестов** widget. An editor can select a course and quiz and render the standard LifterLMS result page inside an Elementor layout; the existing all-results mode remains available. Access-group records (`llms_membership`) are now Elementor-supported as well. The existing `llms-course-builder` is unchanged.

The 0.0.44 source release adds the configurable **Получить сертификат** button to a passed quiz result. It is shown only for the current participant's valid VibeLMS-passed attempt when a certificate is linked and the new **VibeLMS → Настройки → Общие → Кнопка «Получить сертификат»** option is enabled. The option is included in the full VibeLMS transfer archive. The certificate link reuses the existing earned-certificate flow and does not expose another participant's certificate.

On staging, the DOCX-based reusable tests were created through **VibeLMS → Тесты** and are not repository fixtures: published quiz ID 68 is **Тест стандартов «Бифимбилль» — русский язык**, and published quiz ID 86 is **Тест стандартов «Бифимбилль» — казахский язык**. Each contains 15 choice questions with the highlighted DOCX answers entered as correct answers; the Russian quiz is attached to lesson 47 and the Kazakh quiz to lesson 46. A temporary Elementor course page is staging course ID 106, titled **Академия Бифимбилль — стандарты работы (RU)**; it contains the Russian course layout and three YouTube Video widgets, and is used to verify public Elementor output. The test subject may mention Bifimbill, but the plugin remains project-neutral VibeLMS.

The WordPress plugin metadata presents the product as VibeLMS version `0.0.51`, authored by Mazhenov Design with `https://mazhenov.kz` as the plugin site. The internal LifterLMS compatibility version remains `10.2.0` in the core class and is not the public plugin header version. Public VibeLMS updates increment the final numeric segment.

The 0.0.43 maintenance release adds a recovery link when the recording error is already rendered as a server-side paragraph before the interactive quiz UI loads. It also forces the frontend quiz script cache key to the public VibeLMS version, so this recovery code is fetched after deployment. The 0.0.42 idempotent answer-saving and duplicate-completion safeguards remain in place.

The 0.0.42 maintenance release hardens quiz answer saving against duplicate and overlapping AJAX requests. Completed attempts now return their existing result URL instead of a generic recording error, and completion actions are not run twice. The quiz UI locks concurrent submissions, records safe rejection context in opt-in diagnostics, and shows a translated **Начать заново** link for recoverable server/AJAX errors. Quiz scoring, the existing `llms-course-builder` and normal result flow are unchanged; staging acceptance follows this commit.

The 0.0.41 maintenance release fixes quiz-result layout in theme and Elementor contexts. Result cards now constrain their content, keep status icons inside the answer button and wrap long question titles safely. Desktop results use a stable two-column grid instead of float-dependent placement, while the quiz-information block is cleared, boxed and responsive below the results. It now shows the number of completed attempts for the current participant using the existing quiz-attempt counter. The existing course builder and quiz scoring flow are unchanged.

The 0.0.40 maintenance release uses the public VibeLMS version for `LLMS_ASSETS_VERSION` when not in debug mode. This cache-busts the core `llms.min.js` URL after each VibeLMS release, so updated Elementor and course-builder JavaScript is fetched instead of being served under the unchanged internal `10.2.0` key.

The 0.0.39 maintenance release fixes the Elementor editor preview bootstrap: tracking initialization now creates and uses the explicit browser `window.llms` namespace, which prevents `ReferenceError: llms is not defined` when Elementor renders its isolated preview frame. The 0.0.40 package is deployed on staging; the live retest confirmed the VibeLMS widget panel, course preview, `llms.min.js?ver=0.0.40` and zero current editor console errors. The editor iframe can still intermittently receive a staging 502 during a full reload, but the recovered editor renders the course and widgets correctly.

The current staging acceptance pass confirmed that the custom VibeLMS journal records both a 15/15 passed attempt and a 14/15 failed attempt. The 0.0.31 maintenance release refreshes administrator role state and explicitly restores the compatible core LifterLMS report capabilities, so dashboard report links remain usable after Push-to-Deploy. A temporary published certificate template (staging post ID 56) is selected in the settings. The first fresh certificate attempt exposed a server 500 at final submission; the root cause was the 0.0.32 diagnostic code calling `get_id()` on `LLMS_User_Certificate`, which uses the shared `get('id')` model API. The 0.0.33 maintenance release fixes that call.

The follow-up staging setup created published access groups **Языковая группа — Русский** (ID 58) and **Языковая группа — Казахский** (ID 62), assigned them to the corresponding VibeLMS language settings, enabled both WordPress and LifterLMS open registration, and set the default new-user role to **Ученик VibeLMS**. The published Elementor page **Регистрация участника** (staging page ID 7) contains the `[lifterlms_registration]` shortcode. The visitor preview confirms the complete form and the required `Язык обучения` selector with Russian/Kazakh options. An actual user-creation submission is not yet accepted as PASS: the only available browser context remains authenticated as `admin`, and the visitor preview does not provide an isolated anonymous session.

The 0.0.34 maintenance change links a passed attempt to the existing earned certificate ID stored by LifterLMS when the engagement handler returns a duplicate error, while retaining the model-ID fallback for a newly created certificate. Live verification exposed one remaining regression in that change: the journal-linking branch later called `get('id')` on the handler's duplicate-error array, causing a final-submission 500 and leaving `certificate_id` at `0`. The 0.0.35 maintenance release removes that redundant call and preserves the resolved ID for both new and duplicate-safe awards. The 0.0.36 maintenance release adds a null guard to the certificate page copy-link script; the previous live certificate test showed that the download worked but the script emitted a console error when sharing was disabled. The 0.0.33 model-ID fix remains in place.

The 0.0.26 maintenance pass fixed the dashboard activity date range with WordPress `wp_date()` so localized month names follow the site locale instead of PHP's English `date()` output. During the live follow-up, staging exposed the generated screen class `vibelms_page_llms-dashboard`; the 0.0.27 maintenance pass made `postbox` loading match the `llms-dashboard` page query as well as known screen IDs. The 0.0.28 maintenance pass defers the dashboard `postboxes.add_postbox_toggles()` call until the window load event and guards the global API because WordPress prints the core script in the footer.

The 0.0.37 dashboard pass adds project-neutral metric visibility settings to **VibeLMS → Настройки → Общие → Настройки дашборда**. Administrators can show or hide enrollments, registrations, net sales and completed lessons; if every built-in card is unchecked, the dashboard falls back to showing all cards so the screen does not become empty. The dashboard period selector uses the shared reporting date pipeline and supports the last 7, 30 or 90 days, the current month and a validated custom date range. The metric grid reflows to the remaining cards, including on mobile, and the existing `llms-course-builder` is untouched. The 0.0.38 maintenance release disables the custom date inputs while a preset period is selected, keeping preset dashboard URLs clean and unambiguous. Live staging verification on 0.0.40 confirmed all four cards, the default 7-day period, switching to 30 days and zero dashboard console errors.

The first project layer is opt-in diagnostics. It reuses the existing LifterLMS log system and writes structured events to the `vibelms-diagnostics` handle with redaction of common secrets. It records PHP warnings/notices, uncaught throwables and fatal shutdown errors when `VIBELMS_DEBUG` is enabled.

The first VibeLMS-specific feature layer adds the neutral `vibelms_student` and `vibelms_observer` roles. The observer receives only VibeLMS report/export/material capabilities; it does not receive WordPress editor capabilities. Existing installations refresh the administrator report capabilities through the role-version migration.

The repository now includes `scripts/build-installable-package.sh`, which creates a production-style ZIP with Composer runtime dependencies while keeping development dependencies out of the repository. The package build skips Composer dev scripts because those scripts configure PHPCS, which is intentionally absent from a `--no-dev` production install.

The runtime Composer `vendor/` directory is now tracked so the GitHub Push-to-Deploy integration receives the required `vendor/autoload.php`. It contains only production dependencies; development tools remain excluded.

The admin interface now uses VibeLMS branding, shows the public VibeLMS version, hides the old license/support/add-ons/promotional dashboard blocks, and keeps only local content/report shortcuts. The dashboard uses one normal postbox column after the removed promotional side column; this prevents the empty WordPress side-sortables area from creating a large vertical gap. The dashboard report links still target the compatible LifterLMS report screen, so administrator report capabilities must remain present even in simplified mode. Compiled production CSS and JS are tracked because Push-to-Deploy does not execute npm; the missing `admin.css` was the cause of the unstyled dashboard and oversized logo. The customized admin stylesheet now uses `VIBELMS_VERSION` for cache busting.

The admin experience now defaults to a reversible simplified mode. It hides optional legacy menu surfaces such as orders, coupons, engagements, forms, resources, add-ons and system status for non-administrators, without deleting data, changing capabilities or disabling direct URLs. **VibeLMS → Настройки → Общие → Режим интерфейса** switches between **Упрощённый** and **Расширенный** modes. Every VibeLMS admin screen except the existing `llms-course-builder` receives one responsive header with the brand, version, keyboard-visible focus states, active section and quick links to the dashboard, courses, tests, participants, access groups, journal, transfer and settings. Advanced question controls inside the existing course builder remain untouched to preserve compatibility.

The old LifterLMS review-request module was removed from the VibeLMS load path, so the admin footer and review notice no longer contain LifterLMS promotional text or WordPress.org review links. The legacy Add-ons screen is also disabled in the reusable fork. Direct visits to legacy import, setup, resources and add-ons pages are redirected to VibeLMS screens, and the external HelpScout launcher is disabled.

The access-group creation route keeps the internal `post_type=llms_membership` identifier for compatibility, but its visible admin labels are now `Access Groups` / `Группы доступа`. The update-safe role installer refreshes the core post-type capabilities on the first admin request after Push-to-Deploy, so existing active installations regain the capability required to create access groups even when the activation hook did not run.

Generated runtime files required by WordPress are tracked for Push-to-Deploy: `includes/class.llms.l10n.frontend.php` and the production `libraries/lifterlms-blocks/assets/` bundle. They must not be excluded by `.gitignore`, because the deployment server does not run the localization or Gutenberg block build.

The core Gutenberg block build is also tracked in `blocks/`. This includes `blocks/pricing-table/`, which is required for WordPress to recognize `llms/pricing-table` in existing course and access-group content.

VibeLMS remains a reusable LMS engine for different projects. Project-specific branding, languages, slides, videos, documents, companies, questions, passing scores and certificate text must be supplied through configurable content/settings rather than hard-coded client names or assets.

The dashboard analytics enqueue now supports both the legacy LifterLMS screen base and the `toplevel_page_llms-dashboard` screen produced by the VibeLMS menu. The official WordPress.org Russian translation package is included in `languages/` as `lifterlms-ru_RU.po/.mo` plus JavaScript JSON catalogs, and custom VibeLMS admin labels are Russian.

The dashboard analytics path now also recognizes the `page=llms-dashboard` query directly. Widget AJAX starts before Google Charts initialization, and the optional student filter/Google Charts APIs are guarded so a missing optional script cannot leave the metric cards stuck in the loading state. The dashboard's postbox dependency is keyed to the page query, initialization waits for the core script, and localized activity dates use `wp_date()`. The customized analytics asset uses `VIBELMS_VERSION` for cache busting instead of the unchanged internal LifterLMS version.

The 0.0.29 content layer adds the project-neutral `vibelms_material` post type for slides, videos and documents. Each material stores its language, order and optional video URL or media attachment; published materials are rendered only for authenticated users through `[vibelms_materials]` or the Elementor widget **Учебные материалы**. Documents use a nonce-protected `admin-post.php` download route and the `vibelms_can_view_materials` and `vibelms_can_download_material` filters allow projects to add enrollment rules. The default language selector persists the selected language in a secure cookie and the full transfer archive includes material posts, settings, attachments and ID remapping. Direct web-server URLs to files already exposed by the uploads directory remain a deployment-level concern; strict file secrecy requires uploads-server protection or a private storage layer.

The same release adds reusable frontend `[vibelms_header]`, `[vibelms_footer]` and `[vibelms_language_switcher]` shortcodes plus Elementor widgets for the header and footer. A custom WordPress logo is rendered without nested links, and all frontend styles/scripts are versioned with `VIBELMS_VERSION`. The shared admin header now also covers the Materials screen and exposes its quick link to administrators. Transfer user-meta import excludes role/capability and authentication keys even for a valid administrator-uploaded archive.

The universal VibeLMS platform layer stores employee identity fields (`company`, `employee_name`, `region`, `station`) in user meta and exposes them through `[vibelms_student_identity]`. When the optional setting is enabled, quiz access requires these fields. The same form is available as an Elementor widget. The standard registration and guest checkout forms also require a project-extensible learning-language selector (Russian/Kazakh by default), store `vibelms_language`, and can automatically enroll the new participant into the administrator-selected language access group. The language is visible in the protected journal and CSV export.

Completed quiz attempts are copied into the protected `{$wpdb->prefix}vibelms_attempts` table. The journal is available to administrators and observers, supports paginated viewing and UTF-8 CSV export, and stores email, identity snapshot, locale, attempt number, question count, correct answers, grade, status and dates. The table is created on activation and through the update-safe admin migration.

General settings now expose the project-neutral assessment rule: target question count defaults to `15`, passing score defaults to `100%`, optional identity requirement, and an optional certificate template. A successful attempt must match the configured question count and pass threshold before the selected certificate template is awarded. The question builder itself is not limited to 15 questions.

VibeLMS now includes a complete administrator-only transfer screen at **VibeLMS → Перенос данных**. The ZIP archive contains VibeLMS settings, courses with nested lessons/quizzes/questions/access plans, access groups, certificate templates, LMS users, enrollments, quiz attempts, VibeLMS report rows and referenced local media. Import maps users by email and content by the generator's source-ID marker, replaces media URLs, preserves featured images, and adds data without deleting existing records. Passwords, tokens and sensitive user meta are excluded; only newly created users receive a `vibelms_transfer_needs_password_reset` marker. The archive format is versioned and import diagnostics are shown in the admin notice and VibeLMS log when debug mode is enabled.

The transfer UX now stages an uploaded ZIP, validates every required JSON file and shows a preflight report with source site, archive size, object counts, reused/new users, previously imported records and warnings. After confirmation, import runs in bounded AJAX batches with a real progress bar and survives an admin-page refresh. The safe duplicate choices are creating copies or skipping records previously marked with the same source site and source ID; manually created target content is never overwritten. The completion report uses Russian labels for imported/skipped counts, shows bounded warnings and can be downloaded as a short-lived nonce-protected JSON report. A destructive or ambiguous content-update mode is intentionally not enabled yet.

Elementor widget registration supports the modern `elementor/widgets/register` hook with backward compatibility for older Elementor releases. The visible category is `VibeLMS`; the internal category key remains `lifterlms` so saved Elementor documents continue to resolve. The old external Elementor upsell text was removed and replaced with a local VibeLMS note.

Translated UI strings from the compatibility domains replace visible `LifterLMS`/`Lifterlms` product-name text with `VibeLMS`. Internal class names, hooks, post types, shortcodes, text domains and compatibility URLs are intentionally not mass-renamed.

The course builder now uses the public `VIBELMS_VERSION` for its builder CSS, JavaScript and popover assets, preventing stale internal LifterLMS asset URLs after a VibeLMS update. Builder page output records the prepared question-type count and IDs through the existing `vibelms-diagnostics` handle when `VIBELMS_DEBUG` is enabled; this makes an empty “Add Question” panel distinguishable between a PHP data problem and a browser asset/cache problem. The builder no longer renders the upstream tutorial, Question Bank, Events or Assignments upsell URLs; the bundled Question Bank is exposed as a native setting, while unavailable assignments show a local notice.

The 0.0.18 branding sweep removes upstream promotional links from active admin settings, access-plan notices, media protection help, the system report, the WordPress dashboard widget, plugin row metadata, integrations, REST key settings and staging notices. The widget no longer fetches LifterLMS blog or podcast feeds. The course-builder explainer setting and legacy setup/resources templates no longer render upstream links or remote sample-course imports; setup migration notices use local text, and direct legacy URLs are hidden or redirected. Internal compatibility identifiers and API classes remain unchanged.

The course-builder failure `Cannot read properties of undefined (reading 'start')` was caused by the production `llms.js` bundle missing `LLMS.Spinner`, even though builder quiz views call it. The generated `llms-spinner` asset is now tracked and loaded as a dependency of the core `llms` script and the builder, so quiz question creation works on admin and frontend paths that use the shared script.

Advanced Quizzes 3.3.0 is now bundled into VibeLMS rather than installed as a separate plugin. The bundle includes question-type behavior, the question bank, manual-review notifications/reporting, secure file-upload AJAX handling, admin question settings, frontend templates, CodeMirror, Quill Snow styling, and production JS/CSS assets. The existing core question-type definitions remain the compatibility base; the bundled module removes upgrade prompts and attaches the full behavior. If a standalone Advanced Quizzes plugin is already active, VibeLMS reuses its `llms_aq()` integration instead of loading a second copy.

The Russian catalog now translates the True/False labels as `Верно`, `Неверно`, and `Верно или неверно`, and translates Scale as `Шкала`. The 0.0.24 localization pass covers the previously missing course builder, access-plan, reporting, quiz, Advanced Quizzes, media-protection, password-visibility and voucher/export strings. Hard-coded voucher, email, media and default-title labels now use the core catalog or `LLMS.l10n`; language names such as PHP and Python remain intentionally unchanged. Advanced Quizzes now loads its PHP and JavaScript Russian catalogs from the bundled `languages/` directory.

The Advanced Questions buttons are native VibeLMS functionality. Their core definitions now set `upgrade` to `false` directly, so the course builder cannot render them as disabled premium upsells even if a stale cache or delayed add-on initialization is present.

The Elementor integration now exposes a reusable course selector on every course-specific widget, while preserving automatic context on course, lesson and quiz templates. The universal widget set includes course cards/catalogs, lesson lists, quiz rendering, quiz results, certificates, employee profiles, student dashboard and access groups. The `Тест` widget renders a selected quiz through the existing LifterLMS content pipeline and supports a selected course fallback. It sends quiz content through the standard WordPress `the_content` filter so normal formatting, blocks and LifterLMS hooks are preserved; when Elementor renders in an admin context without that filter, it calls the same LifterLMS content handler directly. Every Elementor widget now exposes shared `Стили VibeLMS` controls for typography, colors, background, responsive spacing, border, radius and shadow. Elementor support is enabled for `course`, `lesson` and `llms_quiz` after LifterLMS registers them, and direct admin actions are available for opening those records in Elementor. The existing `llms-course-builder` is not modified; its Finder entry only opens the current course builder in a separate tab for course editing. The compiled `llms-admin-elementor-editor` runtime is shipped with the plugin so this safe entry point works after Push-to-Deploy without a build step.

VibeLMS now has a separate **VibeLMS → Тесты** constructor. It creates and edits reusable quizzes, common multiple-choice questions and answers, passing score, attempts, time limit, results and optional lesson attachment using the existing LLMS models. A standard playable attempt requires attaching the quiz to a lesson; the Elementor widget can select the quiz directly for a reusable layout. Advanced question types remain editable in the full course builder, so the standalone screen does not overwrite them.

## Repository

- Local path: `/Users/diasmazhenov/vibecode/VibeLMS`
- Main branch: `main`
- Upstream reference: `https://github.com/gocodebox/lifterlms`
- Target repository: `https://github.com/DiasMazhenov/VibeLMS`
- The local Git history contains the VibeLMS feature commits on `main`; the original LifterLMS repository is retained as `upstream`.
- A local code-only Graphify index is available in `graphify-out/` for architecture navigation: 17,540 nodes and 28,568 graph edges after clustering. It is intentionally ignored and must not be included in plugin packages or Git commits; the graph is structural, so inferred edges and bundled/vendor code should be verified against source.

## Checks

- PHP syntax and PHPCS must pass for changed PHP files.
- Current checks: PHP lint for all changed PHP files, `node --check assets/js/llms-quiz.js`, `npm run build:styles`, `msgfmt --check`, `git diff --check`, the language registration/enrollment contract check, existing VibeLMS smoke checks, diagnostics and role-definition checks passed; the 0.0.31 role migration also has an explicit administrator report-capability contract, the 0.0.32 certificate diagnostics contract covers missing templates, handler errors, unexpected results and journal write errors, the 0.0.33 certificate model-ID contract covers the fixed award path, the 0.0.34 certificate-link contract covers duplicate-safe journal linking, and the 0.0.35 fix removes the live duplicate-result regression. The 0.0.37 dashboard pass passed PHP lint, shell syntax, source-style compilation and generated-CSS presence checks; live 0.0.40 verification passed metric visibility and the 7/30-day period selector. The 0.0.41 source pass also checks the generated result-grid, status-flow and quiz-information selectors in the production CSS. The 0.0.42 quiz pass checks the completed-attempt redirect, diagnostics fields, submission lock and restart-link contract in source and generated assets. The 0.0.43 pass also checks the server-rendered recovery-link hook and VibeLMS quiz asset cache key. PHPCS is unavailable because the tracked production `vendor/` intentionally excludes `vendor/bin/phpcs`. Legacy JS syntax, the rebuilt quiz asset and minified analytics syntax pass; `assets/css/admin.css`, `assets/js/llms.js` and their production variants are present. The analytics query fallback and optional-library guards are checked before release. Both Russian translation catalogs pass `msgfmt --check`; the Advanced Quizzes catalog is loaded from the bundled `languages/` directory. Full PHPUnit is currently blocked because `vendor/bin/phpunit` and the WordPress test library/database are not installed.
- The 2026-08-27 adversarial review also ran the repository package unit suite: 139 tests passed and 30 failed in 9 suites. The failures are existing snapshot serialization/markup expectations, a React dependency mismatch in the component tests, and upstream `lifterlms` slug/archive-name expectations after the universal VibeLMS rebrand; none exercise the changed Materials navigation or transfer-meta guard. These failures remain a release limitation and must not be reported as a clean full-suite PASS.
- Live review confirmed dashboard recovery, WP Pusher deployment of 0.0.40, Materials CRUD screen rendering, quiz creation beyond 15 questions, protected transfer routes, the passed/failed quiz records, certificate page/download action, journal/CSV link, dashboard period switch and an Elementor editor run with the VibeLMS widgets and course preview. The staging host intermittently returned an upstream 502/`ERR_INCOMPLETE_CHUNKED_ENCODING` during full Elementor reloads; after recovery, the iframe rendered correctly and the current editor error list was empty. Registration configuration is now present: both open-registration switches are enabled, both language groups are assigned, and the form renders in visitor preview. The final registration POST and guest/observer runtime isolation remain unverified because only the administrator browser session is available. Source-level capability and nonce guards passed review.
- Local package artifact: `/Users/diasmazhenov/vibecode/VibeLMS/dist/vibelms-0.0.40.zip` (generated, ignored). It is for staging installation and is not committed to Git.
- Targeted PHPUnit is blocked before test discovery because `tmp/tests/wordpress-tests-lib/includes/functions.php` is not installed. A WordPress test library and database are required to run it.
- `composer.lock`, generated assets and `tmp/` stay untracked according to upstream rules. Runtime `vendor/` is tracked specifically for Push-to-Deploy; dev dependencies must not be installed before committing it.

## Activation incident

The production error log showed `lifterlms.php:48` failing because `/wp-content/plugins/VibeLMS/vendor/autoload.php` was missing. The GitHub source tree previously ignored `vendor/`, so Push-to-Deploy copied an incomplete plugin. The fix is to track the production Composer runtime directory. The same log also contains repeated `WP_DEBUG already defined` warnings in `wp-config.php`; those are a separate server configuration cleanup and are not the activation blocker.
